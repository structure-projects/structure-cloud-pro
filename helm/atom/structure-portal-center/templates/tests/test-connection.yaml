apiVersion: v1
kind: Pod
metadata:
  name: "{{ include \"structure-portal-center.fullname\" . }}-test-connection"
  labels:
    {{- include "structure-portal-center.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['sh', '-c', 'wget --spider -q http://{{ include \"structure-portal-center.fullname\" . }}-portal-console:80']
  restartPolicy: Never
