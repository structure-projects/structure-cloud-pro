apiVersion: v1
kind: Pod
metadata:
  name: "{{ include \"structure-message-center.fullname\" . }}-test-connection"
  labels:
    {{- include "structure-message-center.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['sh', '-c', 'wget --spider -q http://{{ include \"structure-message-center.fullname\" . }}-frontend:80']
  restartPolicy: Never
