apiVersion: v1
kind: Pod
metadata:
  name: "{{ include \"structure-org-center.fullname\" . }}-test-connection"
  labels:
    {{- include "structure-org-center.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['sh', '-c', 'wget --spider -q http://{{ include \"structure-org-center.fullname\" . }}-frontend:80']
  restartPolicy: Never
