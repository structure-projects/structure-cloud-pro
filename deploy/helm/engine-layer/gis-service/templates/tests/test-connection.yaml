apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "gis-service.fullname" . }}-test-connection"
  labels:
    {{- include "gis-service.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "gis-service.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
