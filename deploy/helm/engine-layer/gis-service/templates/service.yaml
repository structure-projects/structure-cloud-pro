apiVersion: v1
kind: Service
metadata:
  name: {{ include "gis-service.fullname" . }}
  labels:
    {{- include "gis-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "gis-service.selectorLabels" . | nindent 4 }}
