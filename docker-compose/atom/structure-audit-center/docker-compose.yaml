version: "3.8"
services:
  audit-service:
    image: registry.cn-hangzhou.aliyuncs.com/structured/audit-service:2.0.0
    restart: always
    pull_policy: always
    hostname: audit-service
    env_file:
      - ../.env
    deploy:
      restart_policy:
        condition: on-failure
      replicas: 1
    networks:
      - structure-cloud-work
    environment:
      - APP_PATH=/app/boot/app.jar
      - JAVA_OPTS=-Xms256m -Xmx1024m
      - PARAMS=-Dfile.encoding=UTF-8 -Dspring.profiles.active=pro -Djava.security.egd=file:/dev/./urandom -Duser.timezone=Asia/Shanghai
    healthcheck:
      test: ["CMD","/bin/sh","/app/liveness.sh"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s
    extra_hosts:
      - "nacos-0.nacos:nacos"
      - "nacos-1.nacos:nacos"
      - "nacos-2.nacos:nacos"

  audit-ui:
    image: registry.cn-hangzhou.aliyuncs.com/structured/audit-ui:2.0.0
    restart: always
    pull_policy: always
    hostname: audit-ui
    env_file:
      - ../.env
    deploy:
      restart_policy:
        condition: on-failure
      replicas: 1
    networks:
      - structure-cloud-work
    environment:
      - SCHEME=https
      - SERVER_HOST=api.prod.structured.cn
      - SERVER_PORT=443
    healthcheck:
      test: ["CMD","curl","-f","http://localhost/"]
      interval: 30s
      timeout: 10s
      retries: 3

networks:
  structure-cloud-work:
    external: true
